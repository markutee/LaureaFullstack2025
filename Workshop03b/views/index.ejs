<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User List</title>
</head>
<body>
    <!-- tehtävä 3 header ja footer templatet -->
    <%- include('_header') %>
    <h1><%= message %></h1>
    <% if (showUsers) { %>
        <ul>
            <% users.forEach(user => { %>
                <li><%= user.name %> - Age: <%= user.age %></li>
            <% }) %>
        </ul>
    <% } else { %>
        <p>No users to display.</p>
    <% } %>
    <%- include('_footer') %>
</body>
</html>
